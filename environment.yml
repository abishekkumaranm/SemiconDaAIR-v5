name: semicondaair
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.12
  - pytorch
  - torchvision
  - pytorch-cuda=12.1
  - pip
  - pip:
      - gradio>=4.0.0
      - opencv-python>=4.8.0
      - numpy>=1.24.0
      - pillow>=9.5.0
      - scipy>=1.10.0
      - pytest>=7.4.0
